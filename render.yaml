services:
  - type: web
    name: fxbreezy-webhook
    env: python
    plan: free
    rootDir: webhook_server
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: TELEGRAM_TOKEN
        sync: false
      - key: TELEGRAM_CHAT_ID
        sync: false
